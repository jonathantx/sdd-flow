# macOS
.DS_Store

# Node (do worker e do fumadocs template, se houver build local)
node_modules/
dist/
build/
.next/
*.log

# Saídas temporárias de teste
/tmp/
*.tmp

# Ambiente
.env
.env.*
!.env.example

# Artefatos de INSTALAÇÃO (gerados pelo install.sh/sdd sync num projeto-alvo).
# O kit-fonte não deve versionar isto — só os assets/ originais.
/.sdd/
/.claude/
/.codex/
/.gemini/
/docs/
/docs-fumadocs/
/scalar/
/docker-compose.sdd.yml
